#!/usr/bin/env bash
# Create a 4096-bit RSA key pair protected by the betty passphrase.

script_dir="$(cd "$(dirname "$0")" && pwd)"

ssh-keygen -t rsa -b 4096 -N betty -f school

cp -f school "$script_dir"/ 2>/dev/null || true
cp -f school.pub "$script_dir"/ 2>/dev/null || true
